{
  "name": "purescript-novel-game-proto",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "app-setup": "pnpm i && spago install && pnpm build",
    "dev": "npm-run-all --parallel dev:*",
    "dev:purs": "spago bundle-app --to assets/app.js -y --watch",
    "dev:vite": "vite",
    "build:purs": "spago bundle-app --to assets/app.js -y",
    "build:vite": "vite build",
    "build": "npm-run-all build:*",
    "preview": "vite preview",
    "test": "spago test"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "npm-run-all": "^4.1.5",
    "purescript": "^0.15.15",
    "purs-tidy": "^0.11.0",
    "spago": "^0.21.0",
    "vite": "^6.0.6"
  }
}
